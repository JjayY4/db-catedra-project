{
  "name": "@project/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch --env-file=../../.env src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@project/auth": "workspace:*",
    "@project/db": "workspace:*",
    "@project/enums": "workspace:*",
    "drizzle-orm": "^0.44.2",
    "elysia": "^1.2.25",
    "inversify": "^6.2.1",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
