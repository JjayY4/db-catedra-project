import { LandingHeroWidget } from '@/widgets/landing-hero'

export default function HomePage() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center bg-slate-50">
      <LandingHeroWidget />
    </main>
  )
}
